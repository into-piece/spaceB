import React, {
  useState,
  useLayoutEffect,
  useEffect,
  useCallback,
} from "react";

interface P {
  history: any;
}

const a = [1, 2, 3];

const Home = (props: P) => {
  return <header className="App-header"></header>;
};

export default Home;
